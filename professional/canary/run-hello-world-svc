#!/bin/bash

SCRIPT=$(readlink -f "$0")
SCRIPTPATH=$(dirname "$SCRIPT")
source ${SCRIPTPATH}/vars
echo "## Press ctrl+c to exit"
echo "##NOTE : You might see Failed to connect message for some time. Be patient"
while true;
do
    sleep 1;
    curl http://${PUB_IP}:${CANARY_PORT}/
done
